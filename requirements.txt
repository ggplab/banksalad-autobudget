# Python 3.10+ · 설치:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#   (uv: uv venv .venv && uv pip install -r requirements.txt --python .venv/bin/python)
# yaml 은 표준 라이브러리로 직접 파싱하므로 PyYAML 불필요. Gmail 은 urllib 로 직접 호출.
openpyxl==3.1.5
